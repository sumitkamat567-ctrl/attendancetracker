name: untitled4
description: "Attendance tracker app with timetable, organizer & undo support"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shimmer: ^3.0.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # 🔹 Local persistent storage (Hive)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # 🔹 Notifications (system / WhatsApp-style)
  flutter_local_notifications: ^17.0.0

  # 🔹 Timezone support (required for scheduled notifications)
  timezone: ^0.9.2

  # 🔹 Image picker (upload timetable image)
  image_picker: ^1.0.7

  # 🔹 OCR (Google ML Kit – for reading timetable image text)
  google_mlkit_text_recognition: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # 🔹 Hive code generation
  hive_generator: ^2.0.1
  build_runner: ^2.4.11

flutter:
  uses-material-design: true
